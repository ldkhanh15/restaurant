# FastAPI and server dependencies (for api_server.py)
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0

# HTTP client (for api_helpers.py and chatbot.py)
requests==2.31.0

# Google Gemini AI (for chatbot.py)
google-generativeai==0.8.0

# Image processing (for chatbot.py)
Pillow==10.4.0

# MIME type detection (for chatbot.py)
python-magic==0.4.27

# Gradio UI (for chatbot.py - optional, only if running Gradio interface)
gradio==4.19.0
