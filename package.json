{
  "name": "restaurant-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync-ngrok": "node ./tools/sync_ngrok.js",
    "generate-env": "node scripts/generate-env.js",
    "generate-env:interactive": "node scripts/generate-env.js --interactive"
  }
}
